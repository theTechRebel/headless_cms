{
  "host": "${process.env.HOST || 'api.dapp.africa'}",
  "port": "${process.env.PORT || 80}",
  "production": true,
  "proxy": {
    "enabled": false
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "path": "/dashboard",
    "autoOpen": false
  }
}
